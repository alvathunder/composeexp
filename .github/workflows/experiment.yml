
name: Experiment

on: [push, workflow_dispatch]

jobs:

  test:
    runs-on: macos-latest

    steps: 
    - name: Check out code
      uses: actions/checkout@v3

    - name: docker
      run: docker-compose -f up -d --build #docker-compose up #--abort-on-container-exit

    - name: print
      run: echo "All done!"

